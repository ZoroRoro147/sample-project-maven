name: maven CI/CD
on: 
 push:
  branches: [ main ]
 pull_request:
  branches: [ main ]

jobs:
 build-and-test:
 runs-on: ubuntu-latest
 steps:
  -name: check file
   uses: actions/checkout@v2
   
  -name: setup version
   uses: actions/setup-java@v2
   with:
    java-version: 11
